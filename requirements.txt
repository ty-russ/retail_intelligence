# ── Core API + data ──────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
pyarrow>=16.0

# ── Streamlit UI ─────────────────────────────────────────────────────────────
streamlit>=1.36
plotly>=5.22
requests>=2.32

# ── LLM SDKs (optional — install only those you have keys for) ───────────────
anthropic>=0.30
openai>=1.30
google-generativeai>=0.7
